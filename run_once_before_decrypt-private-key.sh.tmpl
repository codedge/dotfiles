#!/bin/sh

if [ ! -f "/home/hl/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/hl/.config/chezmoi"
    chezmoi age decrypt --output "/home/hl/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/hl/.config/chezmoi/key.txt"
fi
